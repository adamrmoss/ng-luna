import { InjectionToken } from '@angular/core';

/**
 * Injection token for the tooltip text shown inside `TooltipComponent`.
 */
export const LUNA_TOOLTIP_DATA = new InjectionToken<string>('LUNA_TOOLTIP_DATA');
