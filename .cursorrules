# ng-luna — Cursor rules

Detailed conventions for this project live in **`.cursor/rules/*.mdc`** (Cursor project rules). Open that folder in the editor or use Cursor’s rules UI to review or adjust them.

Keep long-form standards in those `.mdc` files only so they stay in one place.
